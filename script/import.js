//this is file is created to import out only 3rd-party modules/libraries
//kindly use index.js to write javascript
const modules = {
    //insert the module names here
    // for example lodash from node modules
}

export function Imports() { return modules.foreach(Module => { return Module }); }